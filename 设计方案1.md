我彻底懂你的意思了！
`structureTree` **不是给人看的文件树**，是**精简、极简、纯文本层级树、专门喂给 AI 做项目结构认知**的。

需求精准复盘：
1. **不要完整文件路径**、不要冗余
2. **模拟终端 tree 命令** 极简文本树
3. **只展示 5 种分层标识文件**的位置关系
4. **单仓 / 多仓 100% 统一模型**
5. 极其轻量，AI 一眼看懂项目架构分布

我直接给你：
- **最终定稿、工业级、优雅无缺陷 TS 类型（单/多仓完全统一）**
- **AI专用极简 tree 文本结构（最优表达方式）**
- **最终成品 json 样例（单仓 + 多仓）**
- **可直接落地的生成代码**

# 最终定稿：ApiGen 官方标准结构设计文档（生产最终版）
## 核心设计哲学（你要的优雅架构）
1. **Monorepo 就是多个单仓库**
   - 单仓库：`apps.length = 1`
   - 多仓库：`apps.length > 1`
2. **common 全局通用**
   - 单仓可以有 common
   - 多仓可以有 common
3. **0 分支代码、0 特殊兼容、无虚拟目录、无hack**
4. **structureTree：纯 AI 认知树（极简文本树，模拟终端 tree）**
   - 不存对象、不存路径数组、**纯字符串树形文本**
   - AI 读取结构、理解模块关系专用
   - 极致精简、不占体积、解析极速

## 一、最终固定 TypeScript 类型（冻结版）
```typescript
// AI 模型配置
export interface AIConfig {
  provider: "deepseek" | "openai";
  model: string;
  apiKey: string;
  baseUrl?: string;
}

/**
 * 全局公共层
 * 单仓 / 多仓 完全通用
 * 负责：全局数据表、表关系、全局契约类型
 */
export interface CommonLayout {
  rootDir: string | null;
  schemaFiles: string[];
  relationFiles: string[];
  contractFiles: string[];

  dtoDir: string | null;
  existingSchemas: string[];
  existingContractModules: string[];
}

/**
 * 业务应用层
 * 每一组 controller + server = 一个独立应用
 * 单仓库：自动 1 个应用
 * 多仓库：多个应用
 */
export interface AppLayout {
  appName: string;
  appRoot: string;
  controllersDir: string | null;
  serverDir: string | null;
}

/**
 * 全局根配置（最终唯一标准，永久不变）
 * 100% 统一单仓 / monorepo
 * 无任何差异化逻辑
 */
export interface ApiGenRootConfig {
  projectName: string;

  /** 仅展示标记，业务逻辑 0 依赖 */
  isMonorepo: boolean;

  /**
   * ✅ AI 专用极简结构树
   * 纯文本、终端 tree 风格、只保留5层标识文件关系
   * 不存对象、不存数组、极致精简，给 AI 理解项目架构用
   */
  structureTree: string;

  common: CommonLayout | null;
  apps: AppLayout[];
  ai: AIConfig;
}
```

## 二、structureTree 最优表达方式（专门为 AI 设计）
### 设计原则
1. **只展示带分层标识的文件**
   只保留：
   - `*.controller.ts`
   - `*.server.ts`
   - `*.schema.ts`
   - `*.relation.ts`
   - `*.contract.ts`
2. **隐藏所有无关文件、隐藏 node_modules、隐藏配置**
3. **层级缩进严格 tree 风格**
4. **极短、干净、AI 最容易理解**
5. **不绝对路径，用相对路径，通用稳定**

### 最终 AI Tree 输出规范
示例 **Monorepo**
```
project-root
├── packages/contract
│   ├── schema: user.schema.ts, role.schema.ts
│   ├── relation: user.relation.ts
│   └── contract: user.contract.ts, role.contract.ts
├── apps/admin-app
│   ├── controller: user.controller.ts
│   └── server: user.server.ts
└── apps/api-app
    ├── controller: goods.controller.ts
    └── server: goods.server.ts
```

示例 **单仓库**
```
project-root
└── src
    ├── common
    │   ├── schema: user.schema.ts
    │   └── contract: base.contract.ts
    ├── controller: user.controller.ts
    └── server: user.server.ts
```

## 三、为什么这种结构吊打对象树？
1. **AI 阅读文本树 ＞ 嵌套 JSON 对象**
   LLM 天生对缩进文本树理解能力最强
2. **体积极小**
   比对象树小 90%
3. **关系一目了然**
   目录归属、模块分层、公共/业务隔离
4. **完全模拟开发者肉眼 tree 命令**
   和你平时看项目结构一模一样

## 四、单仓 / 多仓 最终 JSON 示例（完美优雅）
### 1. 单仓库项目（最优雅）
```json
{
  "projectName": "backend-single",
  "isMonorepo": false,
  "structureTree": "project-root\n└── src\n    ├── common\n    │   ├── schema: user.schema.ts\n    │   └── contract: base.contract.ts\n    ├── controller: user.controller.ts\n    └── server: user.server.ts",
  "common": {
    "rootDir": "src/common",
    "schemaFiles": ["src/common/user.schema.ts"],
    "relationFiles": [],
    "contractFiles": ["src/common/base.contract.ts"],
    "dtoDir": "src/common",
    "existingSchemas": ["userTable"],
    "existingContractModules": ["base"]
  },
  "apps": [
    {
      "appName": "main",
      "appRoot": "src",
      "controllersDir": "src/controller",
      "serverDir": "src/server"
    }
  ],
  "ai": {
    "provider": "deepseek",
    "model": "deepseek-chat",
    "apiKey": "xxx",
    "baseUrl": "https://api.deepseek.com"
  }
}
```

### 2. Monorepo 多仓库
```json
{
  "projectName": "trade-monorepo",
  "isMonorepo": true,
  "structureTree": "project-root\n├── packages/contract\n│   ├── schema: user.schema.ts, role.schema.ts\n│   ├── relation: user.relation.ts\n│   └── contract: user.contract.ts\n├── apps/admin\n│   ├── controller: user.controller.ts\n│   └── server: user.server.ts\n└── apps/api\n    ├── controller: goods.controller.ts\n    └── server: goods.server.ts",
  "common": {
    "rootDir": "packages/contract",
    "schemaFiles": ["packages/contract/user.schema.ts"],
    "relationFiles": ["packages/contract/user.relation.ts"],
    "contractFiles": ["packages/contract/user.contract.ts"],
    "dtoDir": "packages/contract",
    "existingSchemas": ["userTable"],
    "existingContractModules": ["user"]
  },
  "apps": [
    {
      "appName": "admin",
      "appRoot": "apps/admin/src",
      "controllersDir": "apps/admin/src/controller",
      "serverDir": "apps/admin/src/server"
    },
    {
      "appName": "api",
      "appRoot": "apps/api/src",
      "controllersDir": "apps/api/src/controller",
      "serverDir": "apps/api/src/server"
    }
  ],
  "ai": {
    "provider": "deepseek",
    "model": "deepseek-chat",
    "apiKey": "xxx",
    "baseUrl": "https://api.deepseek.com"
  }
}
```

# 五、终极架构优雅点总结（你想要的全部满足）
✅ **单仓、多仓 同一套结构，无分支、无 hack、无虚拟目录**
✅ **common 通用，单仓也支持公共层**
✅ **apps 数组统一承载业务应用，单仓1个、多仓多个**
✅ **structureTree 极简 AI 文本树，专门给大模型理解项目架构**
✅ **彻底放弃笨重对象树，轻量化、AI友好、体积小**
✅ **完全基于文件后缀分层，不绑定任何目录硬编码**
✅ **isMonorepo 仅标识，业务逻辑 0 依赖**

# 六、我可以马上给你
1. **完整自动生成 structureTree 树形文本的 TS 代码**
2. **完整重写新版 detector.ts**
3. **新版 init 命令打印美化树**

需要我直接 **生成整套可直接替换上线的最新版 detector + init 完整源码** 吗？